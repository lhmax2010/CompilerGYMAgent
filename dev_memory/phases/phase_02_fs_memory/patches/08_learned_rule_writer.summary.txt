Phase 02 / Subtask 2.6 - LearnedRule YAML schema and writer

Requirements:
- REQUIREMENTS.md section 4.2.6
- REQUIREMENTS.md section 4.7.5

Implementation:
- Added LearnedRule schema models for `learned/rules/*.yaml`.
- Added LearnedRule integrity model with fixed excluded fields:
  `integrity`, `user_validated`, and `user_notes`.
- Added canonical learned rule payload/hash helpers:
  `learned_rule_payload`, `compute_learned_rule_payload_hash`,
  `with_learned_rule_integrity`, and `verify_learned_rule_integrity`.
- Added `learned_rule_path`, `write_learned_rule`, and `load_learned_rule`.
- Reused shared `atomic_write_yaml` for learned rule writes.
- Added alias-free bounded YAML loading for learned rule files.
- Exported learned rule models, errors, and helpers from `agent`.

Verification:
- `.venv\Scripts\python.exe -m compileall src\agent` passed.
- `.venv\Scripts\python.exe -m pytest tests/test_fs_memory.py -v` -> 101 passed.
- `.venv\Scripts\python.exe -m pytest -v` -> 259 passed, 0 failed, 1 skipped.
- Manual probe verified write path, excluded fields, user_notes edits, and tamper detection.
