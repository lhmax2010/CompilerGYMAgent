Phase 02 / Subtask 2.7 - Experience YAML schema and writer

Requirements:
- REQUIREMENTS.md section 4.2.6
- REQUIREMENTS.md section 4.3
- REQUIREMENTS.md section 4.4.2
- REQUIREMENTS.md section 4.7.5

Summary:
- Added Experience schema models for local and imported experience YAML,
  including rule, scope, validation counters, audit events, import metadata,
  source integrity, and local integrity.
- Added local/imported consistency checks so imported experiences require
  import metadata and source integrity, while local experiences reject
  imported-only fields.
- Added local integrity helpers and a dotted-field exclusion path for
  compute_payload_hash so validation counters, audit, user notes, and integrity
  blocks can be excluded while semantic rule fields remain tamper-detectable.
- Added alias-free bounded Experience YAML loader, atomic no-overwrite writer,
  path routing for local trust buckets and imported experiences, and public
  exports.

Tests:
- `python -m pytest tests/test_fs_memory.py -v` -> 113 passed.
- `python -m pytest -v` -> 271 passed, 1 skipped on Windows.
