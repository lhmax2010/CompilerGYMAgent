diff --git a/dev_memory/CURRENT_PHASE.yaml b/dev_memory/CURRENT_PHASE.yaml
index 8b5ed4d..33777e3 100644
--- a/dev_memory/CURRENT_PHASE.yaml
+++ b/dev_memory/CURRENT_PHASE.yaml
@@ -1,7 +1,7 @@
 phase_id: phase_06_process_management
 phase_title: "Process Management (runner + cleaner + monitor)"
 started_at: 2026-06-01T15:30:36+08:00
-last_updated: 2026-06-01T20:17:10+08:00
+last_updated: 2026-06-01T20:37:50+08:00
 target_milestone: "M3 (advanced)"
 related_requirements:
   - REQUIREMENTS.md section 3.3.5
@@ -206,7 +206,7 @@ subtasks:
       full: "496 passed"
   - id: "6.5"
     title: "TrialState operation ledger checkpoint schema extension"
-    status: implementation_complete
+    status: done
     related_requirements:
       - ROADMAP.yaml Phase 06 deliverable TrialState operation ledger
       - REQUIREMENTS.md section 3.3.3
@@ -239,5 +239,19 @@ subtasks:
     patch_file: dev_memory/phases/phase_06_process_management/patches/05_trial_operation_ledger.patch
     patch_generated: true
     commit_required: true
-    commit_pending: true
-next_action: "Push Phase 06 Subtask 6.5 implementation for Claude review."
+    commit_pending: false
+    implementation_commit: 4bde11a
+    external_review_verdict: approve
+    external_reviewed_at: 2026-06-01T20:37:50+08:00
+    external_review_tests: "508 passed; targeted checkpoint tests 164 passed"
+    external_review_notes:
+      - "Legacy checkpoints without operations load with empty operations and no current_trial_start_line."
+      - "process_refs path/session/trial validation passed independent review."
+      - "Checkpoint has no integrity hash by design; operation ledger does not require hash recomputation."
+      - "Low-1 process_lab double-fork flaky was resolved by waiting for child pgid/env readiness before returning from the fixture."
+    review_hardening_commit_pending: true
+    ubuntu_validation_status: passed
+    ubuntu_validation_tests:
+      targeted: "164 passed; process_lab/process_cleaner 15 passed"
+      full: "508 passed"
+next_action: "Commit/push 6.5 review sync plus double-fork flaky hardening, then begin Phase 06 Subtask 6.6 doctor/state_consistency.py."
diff --git a/dev_memory/PROGRESS.md b/dev_memory/PROGRESS.md
index 73f6361..b2aa674 100644
--- a/dev_memory/PROGRESS.md
+++ b/dev_memory/PROGRESS.md
@@ -1983,3 +1983,24 @@ Next action: begin Subtask 6.5 TrialState operation ledger.
   - `.venv/bin/python -m pytest tests/ -q` -> 508 passed.
 
 Next action: generate patch artifacts, commit/push Subtask 6.5, then request Claude review.
+
+## 2026-06-01T12:37:50Z - Phase 06 / Subtask 6.5 approved and double-fork flaky hardened
+
+- Claude review verdict: Approve.
+- Review range: `eef6b02..4bde11a`.
+- Review confirmed:
+  - legacy checkpoints without `operations` are backward-compatible,
+  - operation ledgers round-trip through checkpoint YAML,
+  - process refs validate path/session/trial boundaries,
+  - checkpoint has no integrity hash by design, so 6.5 requires no hash recomputation.
+- Addressed Low-1 from review:
+  - process_lab now waits for child pid/pgid/env readiness before returning
+    double-fork and child-process scenarios.
+  - This removes the timing window where cleaner probes could run before the
+    escaped child process was fully visible through `/proc`.
+- Validation:
+  - `.venv/bin/python -m pytest tests/test_fs_memory.py tests/test_identifiers.py -q` -> 164 passed.
+  - `.venv/bin/python -m pytest tests/test_process_lab.py tests/test_process_cleaner.py -q` -> 15 passed.
+  - `.venv/bin/python -m pytest tests/ -q` -> 508 passed.
+
+Next action: begin Subtask 6.6 doctor/state_consistency.py.
diff --git a/dev_memory/phases/phase_06_process_management/CHECKLIST.yaml b/dev_memory/phases/phase_06_process_management/CHECKLIST.yaml
index 4b0ca31..b58cb3d 100644
--- a/dev_memory/phases/phase_06_process_management/CHECKLIST.yaml
+++ b/dev_memory/phases/phase_06_process_management/CHECKLIST.yaml
@@ -38,10 +38,10 @@ subtasks:
         status: done
   - id: "6.5"
     title: "TrialState operation ledger checkpoint schema extension"
-    status: implementation_complete
-    implementation_commit: pending
-    review_status: pending
-    ubuntu_validation_status: pending
+    status: done
+    implementation_commit: 4bde11a
+    review_status: approve
+    ubuntu_validation_status: passed
     checklist:
       - item: "Add CheckpointTrialOperation model"
         status: done
@@ -64,9 +64,11 @@ subtasks:
       - item: "Generate patch artifacts"
         status: done
       - item: "Claude review"
-        status: pending
+        status: done
       - item: "Ubuntu/Linux validation"
-        status: pending
+        status: done
+      - item: "Harden double-fork process_lab readiness after flaky review finding"
+        status: done
   - id: "6.4"
     title: "workspace lock hardening (probe flock + LockStatus unknown)"
     status: done
diff --git a/dev_memory/phases/phase_06_process_management/REVIEW_NOTES.md b/dev_memory/phases/phase_06_process_management/REVIEW_NOTES.md
index 86a6c39..b4eef77 100644
--- a/dev_memory/phases/phase_06_process_management/REVIEW_NOTES.md
+++ b/dev_memory/phases/phase_06_process_management/REVIEW_NOTES.md
@@ -171,3 +171,16 @@ Residual risks / follow-up:
   operation replay policies remain Phase 09/10 scope.
 - 6.7 still wires `current_trial_start_line` into clean trace Layer D
   protection.
+
+External review:
+
+- Verdict: Approve
+- Range: `eef6b02..4bde11a`
+- Notes: operation ledger is backward-compatible, `process_refs` validation
+  covers path/session/trial boundaries, `current_trial_start_line` is required
+  for non-empty operations, and checkpoint has no integrity hash by design.
+- Low finding resolved: the 6.3 double-fork escape test was hardened by waiting
+  for escaped child pgid/env readiness in `process_lab` before cleaner probes
+  run.
+- Validation re-run: checkpoint/identifier targeted set 164 passed,
+  process_lab/process_cleaner targeted set 15 passed, full suite 508 passed.
diff --git a/dev_memory/phases/phase_06_process_management/SUMMARY.md b/dev_memory/phases/phase_06_process_management/SUMMARY.md
index 887a867..e529e99 100644
--- a/dev_memory/phases/phase_06_process_management/SUMMARY.md
+++ b/dev_memory/phases/phase_06_process_management/SUMMARY.md
@@ -216,3 +216,19 @@ Layer D work will use.
 - `tests/test_fs_memory.py tests/test_identifiers.py`: 164 passed
 - `tests/test_errors.py`: 3 passed
 - Full suite: 508 passed
+
+### Review
+
+- Claude review verdict: Approve
+- Review range: `eef6b02..4bde11a`
+- Validation re-run:
+  - checkpoint/identifier targeted set 164 passed
+  - process_lab/process_cleaner targeted set 15 passed
+  - full suite 508 passed
+- Reviewer independently verified:
+  - legacy checkpoints without `operations` load cleanly,
+  - operation ledgers round-trip through YAML,
+  - process refs cannot traverse paths or point at another session/trial,
+  - non-empty operations require `current_trial_start_line`.
+- Low finding resolved: process_lab now waits for escaped child readiness before
+  double-fork cleaner tests probe env/pgid state.
diff --git a/dev_memory/phases/phase_06_process_management/UT_RESULTS.md b/dev_memory/phases/phase_06_process_management/UT_RESULTS.md
index dc0c080..751a246 100644
--- a/dev_memory/phases/phase_06_process_management/UT_RESULTS.md
+++ b/dev_memory/phases/phase_06_process_management/UT_RESULTS.md
@@ -153,3 +153,13 @@ Notes:
 - Targeted tests cover legacy checkpoint migration, operation ledger acceptance,
   required `current_trial_start_line`, YAML round-trip, unsafe process refs,
   session/trial mismatch, duplicate refs, and JSON-only operation details.
+
+Review / validation sync:
+
+- Claude review verdict: Approve
+- Review range: `eef6b02..4bde11a`
+- Re-run checkpoint/identifier targeted tests: 164 passed
+- Re-run process_lab/process_cleaner targeted tests after flaky hardening: 15 passed
+- Re-run full suite: 508 passed
+- Low-1 flaky resolved by polling escaped child readiness before returning
+  from the process_lab fixture.
diff --git a/tests/fixtures/process_lab.py b/tests/fixtures/process_lab.py
index 9a1c944..f0cade1 100644
--- a/tests/fixtures/process_lab.py
+++ b/tests/fixtures/process_lab.py
@@ -328,6 +328,12 @@ class ProcessLab:
         )
         self._tracked_pgids.add(record.pgid)
         self._tracked_pgids.update(child_pgids)
+        _wait_for_child_processes_ready(
+            child_pids=child_pids,
+            child_pgids=child_pgids,
+            session_id=session_id,
+            require_env_marker=include_env_marker,
+        )
         return LabProcess(
             popen=popen,
             record=record,
@@ -396,6 +402,63 @@ def _child_identity(payload: dict[str, Any]) -> tuple[tuple[int, ...], tuple[int
     return (int(payload["child_pid"]),), (int(payload["child_pgid"]),)
 
 
+def _wait_for_child_processes_ready(
+    *,
+    child_pids: tuple[int, ...],
+    child_pgids: tuple[int, ...],
+    session_id: str,
+    require_env_marker: bool,
+    timeout: float = 5.0,
+) -> None:
+    for child_pid, child_pgid in zip(child_pids, child_pgids, strict=True):
+        _wait_for_child_process_ready(
+            child_pid=child_pid,
+            child_pgid=child_pgid,
+            session_id=session_id,
+            require_env_marker=require_env_marker,
+            timeout=timeout,
+        )
+
+
+def _wait_for_child_process_ready(
+    *,
+    child_pid: int,
+    child_pgid: int,
+    session_id: str,
+    require_env_marker: bool,
+    timeout: float,
+) -> None:
+    deadline = time.monotonic() + timeout
+    last_error = "not checked"
+    while time.monotonic() < deadline:
+        try:
+            proc = psutil.Process(child_pid)
+            if proc.status() == psutil.STATUS_ZOMBIE:
+                last_error = "child is zombie"
+                time.sleep(0.01)
+                continue
+            actual_pgid = os.getpgid(child_pid)
+            if actual_pgid != child_pgid:
+                last_error = f"pgid {actual_pgid} != expected {child_pgid}"
+                time.sleep(0.01)
+                continue
+            if require_env_marker:
+                marker = proc.environ().get(AGENT_SESSION_ID_ENV)
+                if marker != session_id:
+                    last_error = f"env marker {marker!r} != {session_id!r}"
+                    time.sleep(0.01)
+                    continue
+            return
+        except (ProcessLookupError, psutil.NoSuchProcess) as exc:
+            last_error = f"child not visible yet: {exc}"
+        except psutil.AccessDenied as exc:
+            last_error = f"child env/metadata access denied: {exc}"
+        time.sleep(0.01)
+    raise RuntimeError(
+        f"timed out waiting for child process {child_pid} readiness: {last_error}"
+    )
+
+
 def _env_marker_visible(
     proc: psutil.Process, session_id: str, *, fallback: bool
 ) -> bool:
