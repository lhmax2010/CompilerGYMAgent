diff --git a/dev_memory/CURRENT_PHASE.yaml b/dev_memory/CURRENT_PHASE.yaml
index 0371bc5..0fa4619 100644
--- a/dev_memory/CURRENT_PHASE.yaml
+++ b/dev_memory/CURRENT_PHASE.yaml
@@ -1,7 +1,7 @@
 phase_id: phase_05_5_integration_feasibility
 phase_title: "Integration Feasibility Mock Spike"
 started_at: 2026-05-30T09:17:16Z
-last_updated: 2026-05-30T09:59:34Z
+last_updated: 2026-05-30T10:03:55Z
 target_milestone: "M1.5 (integration gate)"
 related_requirements:
   - REQUIREMENTS.md section 4.6
@@ -124,4 +124,41 @@ subtasks:
       spike_pytest: "14 passed in 0.02s"
       production_pytest: "451 passed in 1.62s"
       runner: "local .venv + pytest"
-next_action: "Begin Subtask 05.5.3 FullAgentStrategy core: constraint layer + memory + schedule, with good/poor LLM baseline reporting split."
+  - id: "05.5.3"
+    title: "FullAgentStrategy core: constraints + memory + schedule"
+    status: implementation_complete
+    related_requirements:
+      - ROADMAP.yaml Phase 05.5 full-agent strategy goal
+      - ROADMAP.yaml Phase 05.5 constraint layer goal
+      - ROADMAP.yaml Phase 05.5 experience memory goal
+      - ROADMAP.yaml Phase 05.5 good/poor LLM baseline exit criteria
+    files_created:
+      - spikes/05.5_integration_feasibility/full_agent.py
+      - spikes/05.5_integration_feasibility/tests/test_full_agent_strategy.py
+    files_modified:
+      - dev_memory/phases/phase_05_5_integration_feasibility/CHECKLIST.yaml
+      - dev_memory/phases/phase_05_5_integration_feasibility/REVIEW_NOTES.md
+      - dev_memory/phases/phase_05_5_integration_feasibility/SUMMARY.md
+      - dev_memory/phases/phase_05_5_integration_feasibility/UT_RESULTS.md
+      - dev_memory/spikes/05.5_integration_feasibility_findings.md
+      - dev_memory/PROGRESS.md
+      - spikes/05.5_integration_feasibility/README.md
+    behavior_notes:
+      - "FullAgentStrategy combines warmup candidates, MockLLM proposals, pair-jump exploration, local mutation, random fallback, and deterministic enumeration."
+      - "ConstraintLayer rejects empty, duplicate, unknown, conflicting, known-failed-subset, and soft-blocked candidates before spending mock trial budget."
+      - "ExperienceMemory derives tried combos, successes, and known failed subsets from TrialOutcome history."
+      - "Suspicion counter can force a soft-blocked combo after repeated rejection, modeling false-positive recovery."
+      - "Poor LLM scenario: full agent reaches the second-order optimum while LLM-only misses it."
+      - "Good LLM scenario: full agent and LLM-only both reach optimum, but full agent spends zero duplicate trial budget."
+    ut_required: true
+    ut_passed: true
+    targeted_ut: "21 spike tests passed, 0 failed"
+    production_regression_ut: "451 production tests passed, 0 failed"
+    self_review_required: true
+    self_reviewed: true
+    patch_required: true
+    patch_file: dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.patch
+    patch_generated: true
+    commit_required: true
+    commit_pending: true
+next_action: "Generate patch artifacts, commit/push Subtask 05.5.3, then request external review."
diff --git a/dev_memory/PROGRESS.md b/dev_memory/PROGRESS.md
index 9ab88d8..2529b5a 100644
--- a/dev_memory/PROGRESS.md
+++ b/dev_memory/PROGRESS.md
@@ -1657,3 +1657,27 @@ Next action: generate patch artifacts, commit/push Subtask 05.5.2, then request
 - Production `src/agent` remained unchanged.
 
 Next action: sync review records, then begin Subtask 05.5.3 FullAgentStrategy core with scenario-split baseline reporting.
+
+## 2026-05-30T10:03:55Z - Phase 05.5 / Subtask 05.5.3 implemented
+
+- Implemented spike-local `FullAgentStrategy`.
+- Added `ExperienceMemory` derived from prior trial outcomes.
+- Added `ConstraintLayer` for duplicate, unknown option, conflict,
+  known-failed-subset, and soft-blocked candidate rejection before trial budget
+  is spent.
+- Added a suspicion counter that forces a soft-blocked combo after repeated
+  rejection, modeling false-positive recovery.
+- Added a candidate schedule:
+  warmup -> LLM proposal -> generic pair-jump exploration -> local mutation ->
+  random fallback -> deterministic enumeration.
+- Preserved the good/poor LLM comparison split:
+  - good LLM: full agent competes on duplicate-trial efficiency;
+  - poor LLM: full agent competes on fallback robustness / best-score recovery.
+- UT results:
+  - `.venv/bin/python -m pytest spikes/05.5_integration_feasibility/tests -q` -> 21 passed.
+  - `.venv/bin/python -m pytest tests/ -q` -> 451 passed.
+- Probe:
+  - good: full agent 10/10 optimum, duplicate max 0.0; LLM-only duplicate min 0.85.
+  - poor: full agent 10/10 optimum; LLM-only best <= 102.5; local mutation best 108.0.
+
+Next action: generate patch artifacts, commit/push Subtask 05.5.3, then request external review.
diff --git a/dev_memory/phases/phase_05_5_integration_feasibility/CHECKLIST.yaml b/dev_memory/phases/phase_05_5_integration_feasibility/CHECKLIST.yaml
index 9c280e1..bded920 100644
--- a/dev_memory/phases/phase_05_5_integration_feasibility/CHECKLIST.yaml
+++ b/dev_memory/phases/phase_05_5_integration_feasibility/CHECKLIST.yaml
@@ -34,6 +34,35 @@ subtasks:
         status: done
       - item: "Ubuntu/Linux validation"
         status: done
+  - id: "05.5.3"
+    title: "FullAgentStrategy core: constraints + memory + schedule"
+    status: implementation_complete
+    implementation_commit: pending
+    review_status: pending
+    ubuntu_validation_status: pending
+    checklist:
+      - item: "Implement FullAgentStrategy as a spike-local decision core"
+        status: done
+      - item: "Implement ConstraintLayer for duplicate, unknown, conflict, known-failed-subset, and soft-blocked rejection"
+        status: done
+      - item: "Implement ExperienceMemory derived from TrialOutcome history"
+        status: done
+      - item: "Implement suspicion counter forced trial for soft-blocked false positives"
+        status: done
+      - item: "Keep good/poor LLM scenario reporting split: good = efficiency, poor = robustness"
+        status: done
+      - item: "Test poor LLM conflicts and unknown options are rejected before execution"
+        status: done
+      - item: "Test full agent finds the second-order optimum with poor LLM"
+        status: done
+      - item: "Test full agent beats poor LLM-only and local mutation on best score"
+        status: done
+      - item: "Test full agent beats good LLM-only on duplicate trial efficiency"
+        status: done
+      - item: "Run spike tests and production regression tests"
+        status: done
+      - item: "Generate patch artifacts"
+        status: done
   - id: "05.5.2"
     title: "MockLLM + LLMOnlyStrategy + LocalMutationStrategy baselines"
     status: done
diff --git a/dev_memory/phases/phase_05_5_integration_feasibility/REVIEW_NOTES.md b/dev_memory/phases/phase_05_5_integration_feasibility/REVIEW_NOTES.md
index 5964d6c..188caf1 100644
--- a/dev_memory/phases/phase_05_5_integration_feasibility/REVIEW_NOTES.md
+++ b/dev_memory/phases/phase_05_5_integration_feasibility/REVIEW_NOTES.md
@@ -86,3 +86,33 @@ Post-review validation:
 - Spike tests: 14 passed
 - Production regression suite: 451 passed
 - Production `src/agent` changes: none
+
+## Subtask 05.5.3 - FullAgentStrategy Core
+
+Self-review checklist:
+
+- [x] Spike code remains isolated under `spikes/05.5_integration_feasibility/`.
+- [x] No production `src/agent/` files changed.
+- [x] `FullAgentStrategy` is spike-local and intentionally plain Python.
+- [x] `ConstraintLayer` rejects duplicates, unknown options, conflicts, known failed subsets, and soft-blocked candidates before trial execution.
+- [x] `ExperienceMemory` derives tried combos, successes, and failed subsets from prior `TrialOutcome` history.
+- [x] Candidate schedule includes warmup, LLM proposal, pair-jump exploration, local mutation, random fallback, and deterministic enumeration.
+- [x] Pair-jump exploration is generic over option pairs, not hard-coded to `-fA`/`-fB`.
+- [x] Poor LLM conflicts and unknown options do not reach `SyntheticObjective.evaluate`.
+- [x] Poor LLM scenario reaches the second-order optimum.
+- [x] Good LLM scenario compares trial efficiency rather than pretending LLM-only cannot find the optimum.
+- [x] Suspicion counter can force a soft-blocked combo after repeated rejection.
+- [x] Spike tests and production regression tests passed.
+
+Reviewer focus:
+
+- Confirm full-agent success is not just hard-coding the known optimum; the
+  pair-jump generator should be judged as a generic two-option exploration
+  fallback.
+- Confirm against-baseline reporting stays split by scenario:
+  good LLM = efficiency, poor LLM = robustness / best-score recovery.
+- Confirm rejecting duplicates/conflicts/unknowns before execution is a fair
+  simulation of constraint-layer behavior, not silently hiding failed trials.
+- Confirm the suspicion counter is enough foundation for the later false
+  positive recovery test, even though this subtask does not yet implement full
+  bad-experience canary behavior.
diff --git a/dev_memory/phases/phase_05_5_integration_feasibility/SUMMARY.md b/dev_memory/phases/phase_05_5_integration_feasibility/SUMMARY.md
index baaed41..54e7061 100644
--- a/dev_memory/phases/phase_05_5_integration_feasibility/SUMMARY.md
+++ b/dev_memory/phases/phase_05_5_integration_feasibility/SUMMARY.md
@@ -75,3 +75,30 @@ propose the known optimum.
 Follow-up for 05.5.3: report full-agent comparisons by scenario. In the good
 LLM setting, compare trial efficiency against LLM-only; in the poor LLM setting,
 compare robustness and best-score recovery.
+
+## Subtask 05.5.3 - FullAgentStrategy Core
+
+Implemented the first full-agent decision core for the mock spike:
+
+- `ExperienceMemory` extracts tried combos, successful combos, and known failed
+  subsets from prior trial history.
+- `ConstraintLayer` rejects candidates before trial execution when they are:
+  empty, duplicate, unknown, conflicting, supersets of known compile failures,
+  or temporarily soft-blocked.
+- `ConstraintLayer` includes a suspicion counter that forces a soft-blocked
+  combo after repeated rejection, modeling false-positive recovery.
+- `FullAgentStrategy` schedules candidates from:
+  warmup, mock LLM, pair-jump exploration, local mutation, random fallback, and
+  deterministic enumeration.
+- The pair-jump fallback is generic over option pairs and is what lets the
+  full agent escape the local-mutation trap.
+
+### Validation
+
+- Spike tests: 21 passed
+- Production regression suite: 451 passed
+- Scenario probe:
+  - good LLM: full agent and LLM-only both reach optimum, but full agent has
+    duplicate rate 0.0 while LLM-only duplicate rate is at least 0.85.
+  - poor LLM: full agent reaches optimum in 10/10 seeds, while LLM-only misses
+    the second-order optimum and local mutation remains at score 108.
diff --git a/dev_memory/phases/phase_05_5_integration_feasibility/UT_RESULTS.md b/dev_memory/phases/phase_05_5_integration_feasibility/UT_RESULTS.md
index 287fd52..81338f8 100644
--- a/dev_memory/phases/phase_05_5_integration_feasibility/UT_RESULTS.md
+++ b/dev_memory/phases/phase_05_5_integration_feasibility/UT_RESULTS.md
@@ -111,3 +111,41 @@ Result:
 ```text
 451 passed in 1.62s
 ```
+
+## 2026-05-30 - Subtask 05.5.3 FullAgentStrategy Core
+
+Environment:
+
+- Runner: local `.venv`
+- Python: `.venv/bin/python`
+- OS: Linux workspace
+
+Commands:
+
+```bash
+.venv/bin/python -m pytest spikes/05.5_integration_feasibility/tests -q
+```
+
+Result:
+
+```text
+21 passed in 0.04s
+```
+
+```bash
+.venv/bin/python -m pytest tests/ -q
+```
+
+Result:
+
+```text
+451 passed in 1.62s
+```
+
+Probe:
+
+```text
+good: full_hits=10/10, full_score_min=120.0, llm_score_max=120.0, full_dup_max=0.0, llm_dup_min=0.85
+poor: full_hits=10/10, full_score_min=120.0, llm_score_max=102.5, full_dup_max=0.0, llm_dup_min=0.875
+local: best={-O3, -funroll-loops}, score=108.0
+```
diff --git a/dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.review.md b/dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.review.md
new file mode 100644
index 0000000..89dc2d7
--- /dev/null
+++ b/dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.review.md
@@ -0,0 +1,48 @@
+# Self Review - Phase 05.5 / Subtask 05.5.3 FullAgent Core
+
+## Scope
+
+This subtask adds the spike-local full-agent decision core: lightweight
+constraints, experience memory, and a candidate schedule. It does not implement
+real gbs, real LLM APIs, process management, workspace protection, LangGraph, or
+production `src/agent` behavior.
+
+## Checks
+
+- Spike code remains isolated under `spikes/05.5_integration_feasibility/`.
+- No production `src/agent/` files changed.
+- `FullAgentStrategy` is a plain Python spike strategy.
+- `ConstraintLayer` rejects duplicates, unknown options, conflicts, known
+  failed subsets, and soft-blocked candidates before execution.
+- `ExperienceMemory` is derived only from trial history.
+- Candidate schedule is explicit:
+  warmup -> LLM -> pair-jump -> local mutation -> random -> enumeration.
+- Pair-jump exploration is generic over option pairs, not hard-coded to
+  `-fA`/`-fB`.
+- Poor LLM conflicts and unknown options do not produce compile-failed trials.
+- Poor LLM scenario reaches the known second-order optimum.
+- Good LLM scenario compares efficiency: full agent has duplicate rate 0.0
+  while LLM-only repeats heavily.
+- Suspicion counter provides the first false-positive recovery hook.
+
+## Reviewer Focus
+
+- Confirm full-agent success is not just a hard-coded answer path.
+- Confirm good/poor LLM comparisons are reported separately:
+  good LLM = trial efficiency, poor LLM = robustness and best-score recovery.
+- Confirm constraint rejections before execution are fair for this spike and do
+  not hide actual objective failures that should consume budget.
+- Confirm the suspicion counter is a reasonable minimal foundation for the
+  later bad-experience / Canary test.
+
+## Validation
+
+```bash
+.venv/bin/python -m pytest spikes/05.5_integration_feasibility/tests -q
+# 21 passed in 0.04s
+```
+
+```bash
+.venv/bin/python -m pytest tests/ -q
+# 451 passed in 1.62s
+```
diff --git a/dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.summary.txt b/dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.summary.txt
new file mode 100644
index 0000000..bfce079
--- /dev/null
+++ b/dev_memory/phases/phase_05_5_integration_feasibility/patches/03_full_agent_core.summary.txt
@@ -0,0 +1,48 @@
+Subtask 05.5.3 - FullAgentStrategy Core
+
+Implemented the first full-agent decision core for the 05.5 mock integration
+spike:
+
+- Added ExperienceMemory:
+  - derives tried combos from TrialOutcome history
+  - records successful combos
+  - records compile-failed combos as known failed subsets
+
+- Added ConstraintLayer:
+  - rejects empty combos
+  - rejects duplicate combos before spending trial budget
+  - rejects unknown options
+  - rejects OptionV0 conflicts
+  - rejects supersets of known compile failures
+  - supports soft-blocked combos with a suspicion counter
+  - forces a soft-blocked combo after repeated rejection to model false-positive
+    recovery
+
+- Added FullAgentStrategy:
+  - warmup candidates
+  - MockLLM proposal
+  - generic pair-jump exploration
+  - local mutation fallback
+  - random fallback
+  - deterministic enumeration fallback
+
+- Added tests showing:
+  - poor LLM conflicts and unknown options do not reach execution
+  - full agent finds the second-order optimum with poor LLM
+  - full agent beats poor LLM-only and local mutation on best score
+  - full agent beats good LLM-only on duplicate-trial efficiency
+  - known failed subsets are filtered
+  - suspicion counter forces a soft-blocked combo
+  - duplicate pressure is rejected before trial budget is spent
+
+Validation:
+
+- .venv/bin/python -m pytest spikes/05.5_integration_feasibility/tests -q -> 21 passed
+- .venv/bin/python -m pytest tests/ -q -> 451 passed
+
+Scenario probe:
+
+- good LLM: full agent hits optimum 10/10 and duplicate max is 0.0; LLM-only
+  also hits optimum but duplicate rate is at least 0.85
+- poor LLM: full agent hits optimum 10/10; LLM-only misses the second-order
+  optimum; local mutation remains stuck at score 108
diff --git a/dev_memory/spikes/05.5_integration_feasibility_findings.md b/dev_memory/spikes/05.5_integration_feasibility_findings.md
index 76d0b54..27b083d 100644
--- a/dev_memory/spikes/05.5_integration_feasibility_findings.md
+++ b/dev_memory/spikes/05.5_integration_feasibility_findings.md
@@ -47,25 +47,25 @@ local-mutation, and full-agent strategies are implemented.
 
 | Strategy | Seeds | Top-5% hit rate | Median best score | Duplicate trial rate | Notes |
 |---|---:|---:|---:|---:|---|
-| RandomStrategy | TBD | TBD | TBD | TBD | TBD |
-| LLMOnlyStrategy | TBD | TBD | TBD | TBD | TBD |
-| LocalMutationStrategy | TBD | TBD | TBD | TBD | TBD |
-| FullAgentStrategy | TBD | TBD | TBD | TBD | TBD |
+| RandomStrategy | TBD | TBD | TBD | TBD | Implemented in 05.5.1 |
+| LLMOnlyStrategy | 10-probe | good reaches optimum; poor misses | good=120 / poor<=102.5 | >=0.85 | Implemented in 05.5.2 |
+| LocalMutationStrategy | 10-probe | 0% | 108.0 | 0.0 | Stable local optimum baseline |
+| FullAgentStrategy | 10-probe | 100% good and poor | 120.0 | 0.0 | Implemented in 05.5.3 |
 
 ## Test Matrix
 
 | # | Test | Result | Notes |
 |---:|---|---|---|
-| 1 | Constraint blocks impossible combos (`-O3 + -Os` actual execution count = 0) | TBD | |
+| 1 | Constraint blocks impossible combos (`-O3 + -Os` actual execution count = 0) | Partial pass | FullAgentStrategy rejects poor-LLM conflicts before execution; full test matrix still pending |
 | 2 | Experience reduces repeats (failed combo not retried) | TBD | |
-| 3 | 30-50 round convergence (full agent finds top-5%) | TBD | |
-| 4 | Poor LLM fallback still improves | TBD | |
-| 5 | False-positive recovery via suspicion counter | TBD | |
-| 6 | Full agent beats random / LLM-only / local-mutation | TBD | |
+| 3 | 30-50 round convergence (full agent finds top-5%) | Partial pass | Noiseless full agent reaches known optimum in 10/10 seeds |
+| 4 | Poor LLM fallback still improves | Partial pass | Poor LLM-only misses optimum; full agent reaches it via fallback |
+| 5 | False-positive recovery via suspicion counter | Partial pass | ConstraintLayer forces soft-blocked combo after threshold; canary behavior pending |
+| 6 | Full agent beats random / LLM-only / local-mutation | Partial pass | Beats poor LLM-only/local on score and good LLM-only on duplicate efficiency; random multi-seed comparison pending |
 | 7 | Noisy benchmark + infra failures do not become hard bad experience | TBD | |
 | 8 | Bad hard rule is Canary-downgraded or does not permanently block optimum | TBD | |
-| 9 | Duplicate LLM pressure rejected without spending trial budget | TBD | |
-| 10 | Full agent learns second-order `-fA`/`-fB`; local mutation gets stuck | TBD | |
+| 9 | Duplicate LLM pressure rejected without spending trial budget | Partial pass | FullAgentStrategy duplicate rate 0.0 and records duplicate rejections |
+| 10 | Full agent learns second-order `-fA`/`-fB`; local mutation gets stuck | Partial pass | Full agent finds optimum; local mutation remains at `{-O3, -funroll-loops}` |
 | 11 | Crash/resume preserves round/history/experience state | TBD | |
 
 ## Implementation Log
@@ -126,6 +126,29 @@ Review:
   scenario. Good LLM should emphasize trial efficiency; poor LLM should
   emphasize fallback robustness and best-score recovery.
 
+### 2026-05-30 - Subtask 05.5.3 FullAgentStrategy Core
+
+Implemented:
+
+- `ExperienceMemory` derived from prior `TrialOutcome` history.
+- `ConstraintLayer` for duplicate, unknown-option, conflict,
+  known-failed-subset, and soft-blocked candidate rejection.
+- Suspicion counter that forces a soft-blocked combo after repeated rejection.
+- `FullAgentStrategy` schedule:
+  warmup -> mock LLM -> generic pair-jump exploration -> local mutation ->
+  random fallback -> deterministic enumeration.
+
+Validation:
+
+- Poor LLM conflicts and unknown options are rejected before execution:
+  actual compile failures are 0 in the full-agent run.
+- Full agent reaches the second-order optimum in 10/10 noiseless poor-LLM
+  seeds, while poor LLM-only misses it and local mutation stays at 108.
+- Good LLM-only can also reach the optimum, so the comparison is efficiency:
+  full agent duplicate rate 0.0 vs LLM-only duplicate rate >= 0.85 in the probe.
+- `spikes/05.5_integration_feasibility/tests`: 21 passed.
+- Production `tests/`: 451 passed.
+
 ## Exit Criteria
 
 - Full agent finds top-5% optimum within 30-50 rounds in at least 80% of seeds.
diff --git a/spikes/05.5_integration_feasibility/README.md b/spikes/05.5_integration_feasibility/README.md
index 943d41d..6374447 100644
--- a/spikes/05.5_integration_feasibility/README.md
+++ b/spikes/05.5_integration_feasibility/README.md
@@ -28,6 +28,8 @@ benchmarks, bad experience, duplicate pressure, and crash/resume?
 - `mock_llm.py`: deterministic controllable fake LLM with good/poor quality.
 - `strategies.py`: random, LLM-only, and local-mutation baselines plus trial
   outcome record.
+- `full_agent.py`: spike-local full-agent strategy with lightweight
+  constraints, memory, schedule, and false-positive suspicion counter.
 - `runner.py`: seeded plain Python strategy runner.
 - `tests/`: spike-only pytest coverage.
 
diff --git a/spikes/05.5_integration_feasibility/full_agent.py b/spikes/05.5_integration_feasibility/full_agent.py
new file mode 100644
index 0000000..b2c9c1b
--- /dev/null
+++ b/spikes/05.5_integration_feasibility/full_agent.py
@@ -0,0 +1,196 @@
+"""Minimal full-agent strategy for the 05.5 integration spike."""
+
+from __future__ import annotations
+
+import itertools
+import random
+from dataclasses import dataclass, field
+from typing import Literal
+
+from mock_llm import MockLLM
+from option_ir import DEFAULT_OPTIONS, OptionV0, option_by_raw, raw_options
+from strategies import TrialOutcome
+
+
+RejectReason = Literal[
+    "empty",
+    "duplicate",
+    "unknown_option",
+    "conflict",
+    "known_failed_subset",
+    "soft_blocked",
+]
+
+
+@dataclass(frozen=True)
+class RejectedCandidate:
+    """One candidate rejected before spending mock trial budget."""
+
+    combo: frozenset[str]
+    reason: RejectReason
+
+
+@dataclass(frozen=True)
+class ExperienceMemory:
+    """Small memory extracted from prior trial outcomes."""
+
+    tried_combos: frozenset[frozenset[str]]
+    successful_combos: tuple[frozenset[str], ...]
+    known_failed_subsets: frozenset[frozenset[str]]
+
+    @classmethod
+    def from_history(cls, history: list[TrialOutcome]) -> ExperienceMemory:
+        failed = frozenset(
+            trial.combo
+            for trial in history
+            if trial.result.outcome == "compile_failed"
+        )
+        successes = tuple(
+            trial.combo
+            for trial in history
+            if trial.result.succeeded
+        )
+        return cls(
+            tried_combos=frozenset(trial.combo for trial in history),
+            successful_combos=successes,
+            known_failed_subsets=failed,
+        )
+
+
+@dataclass
+class ConstraintLayer:
+    """Reject candidates that should not consume mock trial budget."""
+
+    options: tuple[OptionV0, ...] = DEFAULT_OPTIONS
+    suspicion_threshold: int = 3
+    soft_blocked: frozenset[frozenset[str]] = frozenset()
+    suspicion_counts: dict[frozenset[str], int] = field(default_factory=dict)
+    forced_candidates: list[frozenset[str]] = field(default_factory=list)
+
+    def validate(
+        self,
+        combo: frozenset[str],
+        *,
+        memory: ExperienceMemory,
+    ) -> RejectReason | None:
+        """Return a rejection reason, or None when the combo may execute."""
+
+        if not combo:
+            return "empty"
+        if combo in memory.tried_combos:
+            return "duplicate"
+        known_by_raw = option_by_raw(self.options)
+        if combo - set(known_by_raw):
+            return "unknown_option"
+        if self._has_conflict(combo, known_by_raw):
+            return "conflict"
+        if any(failed <= combo for failed in memory.known_failed_subsets):
+            return "known_failed_subset"
+        if combo in self.soft_blocked and combo not in self.forced_candidates:
+            count = self.suspicion_counts.get(combo, 0) + 1
+            self.suspicion_counts[combo] = count
+            if count >= self.suspicion_threshold:
+                self.forced_candidates.append(combo)
+                return None
+            return "soft_blocked"
+        return None
+
+    def _has_conflict(
+        self,
+        combo: frozenset[str],
+        known_by_raw: dict[str, OptionV0],
+    ) -> bool:
+        raw_by_id = {option.option_id: option.raw for option in known_by_raw.values()}
+        for raw in combo:
+            option = known_by_raw[raw]
+            conflict_raws = {
+                raw_by_id[conflict_id]
+                for conflict_id in option.conflicts
+            }
+            if combo & conflict_raws:
+                return True
+        return False
+
+
+@dataclass
+class FullAgentStrategy:
+    """Spike-local decision core: LLM + constraints + memory + exploration."""
+
+    options: tuple[OptionV0, ...] = DEFAULT_OPTIONS
+    llm: MockLLM = field(default_factory=lambda: MockLLM(quality="poor"))
+    constraint_layer: ConstraintLayer | None = None
+    max_random_fallbacks: int = 24
+    rejections: list[RejectedCandidate] = field(default_factory=list)
+
+    def __post_init__(self) -> None:
+        if self.constraint_layer is None:
+            self.constraint_layer = ConstraintLayer(options=self.options)
+
+    def propose(
+        self,
+        history: list[TrialOutcome],
+        rng: random.Random,
+    ) -> frozenset[str]:
+        memory = ExperienceMemory.from_history(history)
+        for combo in self._candidate_stream(history, memory, rng):
+            reason = self.constraint_layer.validate(combo, memory=memory)
+            if reason is None:
+                return combo
+            self.rejections.append(RejectedCandidate(combo=combo, reason=reason))
+        raise RuntimeError("FullAgentStrategy exhausted candidate stream")
+
+    def _candidate_stream(
+        self,
+        history: list[TrialOutcome],
+        memory: ExperienceMemory,
+        rng: random.Random,
+    ):
+        del memory
+        yield from self._warmup_candidates()
+        yield self.llm.propose(history, rng)
+        yield from self._pair_jump_candidates(history)
+        yield from self._local_mutation_candidates(history)
+        yield from self._random_candidates(rng)
+        yield from self._enumerated_candidates()
+
+    def _warmup_candidates(self):
+        yield frozenset({"-O3"})
+        yield frozenset({"-O3", "-funroll-loops"})
+
+    def _pair_jump_candidates(self, history: list[TrialOutcome]):
+        center = self._best_successful_combo(history)
+        raws = tuple(sorted(raw_options(self.options)))
+        missing = tuple(raw for raw in raws if raw not in center)
+        for first, second in itertools.combinations(missing, 2):
+            yield frozenset((*center, first, second))
+
+    def _local_mutation_candidates(self, history: list[TrialOutcome]):
+        center = self._best_successful_combo(history)
+        for raw in tuple(sorted(raw_options(self.options))):
+            if raw in center:
+                yield frozenset(value for value in center if value != raw)
+            else:
+                yield frozenset((*center, raw))
+
+    def _random_candidates(self, rng: random.Random):
+        raws = tuple(raw_options(self.options))
+        for _ in range(self.max_random_fallbacks):
+            combo = frozenset(raw for raw in raws if rng.random() < 0.35)
+            if combo:
+                yield combo
+
+    def _enumerated_candidates(self):
+        raws = tuple(sorted(raw_options(self.options)))
+        for size in range(1, min(4, len(raws)) + 1):
+            for combo in itertools.combinations(raws, size):
+                yield frozenset(combo)
+
+    def _best_successful_combo(
+        self,
+        history: list[TrialOutcome],
+    ) -> frozenset[str]:
+        successes = [trial for trial in history if trial.result.succeeded]
+        if not successes:
+            return frozenset()
+        best = max(successes, key=lambda trial: trial.result.score or float("-inf"))
+        return best.combo
diff --git a/spikes/05.5_integration_feasibility/tests/test_full_agent_strategy.py b/spikes/05.5_integration_feasibility/tests/test_full_agent_strategy.py
new file mode 100644
index 0000000..044867c
--- /dev/null
+++ b/spikes/05.5_integration_feasibility/tests/test_full_agent_strategy.py
@@ -0,0 +1,151 @@
+from __future__ import annotations
+
+from mock_llm import MockLLM
+from objective import ScoreResult, SyntheticObjective
+from option_ir import DEFAULT_OPTIONS
+from runner import run_strategy
+from strategies import LLMOnlyStrategy, LocalMutationStrategy, TrialOutcome
+from full_agent import ConstraintLayer, ExperienceMemory, FullAgentStrategy
+
+
+def objective_without_noise() -> SyntheticObjective:
+    return SyntheticObjective(noise_sigma=0.0, infra_fail_rate=0.0)
+
+
+def test_full_agent_blocks_poor_llm_conflicts_and_unknowns_before_execution() -> None:
+    objective = objective_without_noise()
+    strategy = FullAgentStrategy(llm=MockLLM(quality="poor"))
+
+    result = run_strategy(strategy, objective, rounds=30, seed=5)
+    known = {
+        "-O3",
+        "-Os",
+        "-funroll-loops",
+        "-fA",
+        "-fB",
+        "-ffast-math",
+        "-fno-plt",
+        "-flto",
+    }
+
+    assert result.compile_failed_count == 0
+    assert all(trial.combo <= known for trial in result.trials)
+    assert {"conflict", "unknown_option", "duplicate"} <= {
+        rejection.reason for rejection in strategy.rejections
+    }
+
+
+def test_full_agent_finds_second_order_optimum_with_poor_llm() -> None:
+    objective = objective_without_noise()
+    hits = 0
+    for seed in range(10):
+        strategy = FullAgentStrategy(llm=MockLLM(quality="poor"))
+        result = run_strategy(strategy, objective, rounds=40, seed=seed)
+        hits += int(
+            result.best_trial is not None
+            and result.best_trial.combo == objective.known_optimum
+        )
+
+    assert hits >= 8
+
+
+def test_full_agent_beats_poor_llm_only_and_local_mutation() -> None:
+    objective = objective_without_noise()
+
+    full = run_strategy(
+        FullAgentStrategy(llm=MockLLM(quality="poor")),
+        objective,
+        rounds=40,
+        seed=3,
+    )
+    llm_only = run_strategy(
+        LLMOnlyStrategy(MockLLM(quality="poor")),
+        objective,
+        rounds=40,
+        seed=3,
+    )
+    local = run_strategy(
+        LocalMutationStrategy(DEFAULT_OPTIONS),
+        objective,
+        rounds=40,
+        seed=3,
+    )
+
+    assert full.best_trial is not None
+    assert full.best_trial.combo == objective.known_optimum
+    assert full.best_score == objective.optimum_score(module="core")
+    assert llm_only.best_score is not None
+    assert local.best_score is not None
+    assert full.best_score > llm_only.best_score
+    assert full.best_score > local.best_score
+
+
+def test_full_agent_good_llm_wins_efficiency_against_llm_only() -> None:
+    objective = objective_without_noise()
+
+    full = run_strategy(
+        FullAgentStrategy(llm=MockLLM(quality="good")),
+        objective,
+        rounds=24,
+        seed=9,
+    )
+    llm_only = run_strategy(
+        LLMOnlyStrategy(MockLLM(quality="good")),
+        objective,
+        rounds=24,
+        seed=9,
+    )
+
+    assert full.best_trial is not None
+    assert llm_only.best_trial is not None
+    assert full.best_trial.combo == objective.known_optimum
+    assert llm_only.best_trial.combo == objective.known_optimum
+    assert full.duplicate_trial_rate == 0.0
+    assert llm_only.duplicate_trial_rate > 0.5
+    assert full.unique_combo_count > llm_only.unique_combo_count
+
+
+def test_constraint_layer_filters_known_failed_subsets() -> None:
+    failed = frozenset({"-ffast-math"})
+    memory = ExperienceMemory.from_history(
+        [
+            TrialOutcome(
+                round_index=0,
+                combo=failed,
+                module="core",
+                result=ScoreResult(outcome="compile_failed", reason="conflict"),
+            )
+        ]
+    )
+    layer = ConstraintLayer()
+
+    reason = layer.validate(
+        frozenset({"-O3", "-ffast-math"}),
+        memory=memory,
+    )
+
+    assert reason == "known_failed_subset"
+
+
+def test_suspicion_counter_forces_soft_blocked_candidate_once() -> None:
+    optimum = SyntheticObjective().known_optimum
+    layer = ConstraintLayer(
+        soft_blocked=frozenset({optimum}),
+        suspicion_threshold=3,
+    )
+    memory = ExperienceMemory.from_history([])
+
+    assert layer.validate(optimum, memory=memory) == "soft_blocked"
+    assert layer.validate(optimum, memory=memory) == "soft_blocked"
+    assert layer.validate(optimum, memory=memory) is None
+    assert layer.forced_candidates == [optimum]
+
+
+def test_full_agent_does_not_spend_budget_on_duplicates() -> None:
+    objective = objective_without_noise()
+    strategy = FullAgentStrategy(llm=MockLLM(quality="good"))
+
+    result = run_strategy(strategy, objective, rounds=30, seed=7)
+
+    assert result.duplicate_trial_rate == 0.0
+    assert any(rejection.reason == "duplicate" for rejection in strategy.rejections)
